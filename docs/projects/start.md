# start

**The template.** The starter project that proves the template works end to end: uv with the uv_build backend, a src layout, ruff, mypy and pyright in strict mode, pytest with full branch coverage, and a single runnable example. Copy it when adding a new project.

```bash
cd start
make install
make run-all
```

1 example, in phases:

